# Audit .md di progetto — 2026-08-31

Triage dei 5 file di documentazione "di progetto" (esclusi audit storici, plans/specs,
e i file in `giocatori/`/`grafica/` che sono dati/contenuto reale, non documentazione).

Verificato contro lo stato attuale del repo (struttura cartelle, `pipeline/`, file tracciati).
Nessuna riscrittura eseguita — solo triage, come richiesto. Il rewrite lo fa Opus.

---

## PULITI — nessuna riscrittura necessaria

### `README.md`
Verificato riga per riga contro la struttura reale del repo: stack, comandi (`streamlit run
dashboard/app.py`, `python pipeline/scheduled_run.py`, `pytest`), e l'elenco cartelle
(`dashboard/`, `scrapers/`, `pipeline/`, `matching/`, `ranking/`, `db/`, `tests/`, `fixtures/`,
`scripts/`, `data/`, `docs/`, `giocatori/`, `grafica/`) corrispondono tutti a quanto presente
su disco. Unico gap minore: esiste anche `consensus/` (motore di consenso tra fonti) non
elencato nella struttura — aggiunta di una riga, non una riscrittura.

### `docs/task_scheduler_setup.md`
Breve, mono-argomento, verificato che `pipeline/scheduled_run.py` esiste davvero. Ancora
accurato.

---

## DA RIFARE (in caso) — candidati per Opus

### `visione-progetto.md` — 5039 righe
Non è "sbagliato": è la visione fondativa/aspirazionale del progetto, esplicitamente non
pensata per descrivere lo stato attuale. Ma è cresciuto per accumulo — in fondo al file c'è
una sezione "AGGIORNAMENTO — EVOLUZIONE DEL PROGETTO" (§1-2, poi salta a §106-107) appesa
in coda invece che integrata nel corpo, con numerazione delle sezioni ora incoerente.
Candidato naturale per una riorganizzazione/consolidamento — non perché il contenuto sia
falso, ma perché la struttura è diventata difficile da navigare.

### `AGENTS.md` e `claude.md`
**Sono byte-identici** (stesse regole operative, stesso contenuto). Non serve "riscriverli",
serve decidere quale dei due è la fonte canonica ed eliminare il duplicato (o rendere uno
un semplice riferimento all'altro), altrimenti ogni futura modifica alle regole rischia di
essere fatta su una sola copia e non sull'altra.

---

## Non incluso in questo audit (per scelta)

Questi restano fuori scope perché non sono "documentazione di progetto" nel senso di
descrivere architettura/come-si-usa:

- `OPUS_PROJECT_REVIEW.md`, `AUDIT_2026-08-30_CORREZIONI.md`, `AUDIT_2026-08-31.md`,
  `AUDIT_DATI_2026-08-30.md` — record storici di audit passati, cancellarli perde la
  tracciabilità di cosa fu trovato/corretto e quando.
- `docs/superpowers/plans/*`, `docs/superpowers/specs/*` — piani/design storici di feature
  già implementate, stesso motivo.
- `giocatori/*.md`, `grafica/grafica.md`, `impossibile-*.md` — contenuto/criteri reali del
  progetto (rosa, criteri selezione), non rigenerabile dal codice.

## Nota a margine (fuori scope .md)

Trovati due file binari tracciati in git che sembrano scarti di lavorazione, non output del
progetto: `ruff-errors.txt` (root) e `griglia-portieri-2026-2027-fantacalcio.png` (root).
Non li ho toccati — segnalo solo, decidi tu se vanno rimossi/gitignorati.
