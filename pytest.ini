[pytest]
testpaths = tests
pythonpath = .
markers =
    realdb: runs against data/fantacalcio.db (skipped automatically when that file is missing)
    live: fetches a real external site to check the scraper's selectors still match production markup — excluded from the default run, opt in with `-m live`
addopts = -m "not live"
